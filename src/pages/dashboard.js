import React from "react";
import { Paper, Container } from "@mui/material";

const dashboard = () => {
  return (
    <Paper elevation={2}>
      <Container top={10}>
        <div>Dashboard</div>
      </Container>
    </Paper>
  );
};

export default dashboard;
